export const LOADING = {
  loading: true,
  failure: false,
  success: false,
};

export const SUCCESS = {
  loading: false,
  failure: false,
  success: true,
};

export const FAILURE = {
  loading: false,
  failure: true,
  success: false,
};
